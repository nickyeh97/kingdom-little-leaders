# CLAUDE.md

本文件提供給 Claude Code（及所有 AI 協作者）在此 repo 工作時的指引。

## 語言慣例

- 與使用者的對話、文件、commit 說明主體，一律使用**繁體中文**（台灣用語）。
- 程式碼識別字（變數、函式、資料表欄位）使用英文；註解可用中文。

## 專案概觀

**兒童主日學整合平台（Web APP / PWA）**，隸屬於 TBOJ（The Book of James）信仰實踐計畫（雅各書 1:27、2:17），總計畫願景見主 repo [`nickyeh97/TBOJ`](https://github.com/nickyeh97/TBOJ)。

以單一平台整合：出席預先統計、現場簽到、老師備課資源、敬拜歌單佈達、服事經歷卡、家長公告與雙向互動。

> 完整需求規格：`docs/SUNDAY_SCHOOL_PLATFORM_SPEC.md`（需求基準，開發前必讀）。

### ⚠️ 聚會時間（重要，勿假設）

- 本教會的**主日聚會在週六下午舉行，不是週日**；且聚會日**沒有強制固定在週幾**。
- 聚會日與預先出席截止日為**可設定值**（`src/lib/config.ts`：`GATHERING_WEEKDAY`、`PLAN_DEADLINE_DAYS_BEFORE`），程式與資料庫一律以 `gathering` 命名，**不得在任何地方寫死 Sunday／週日**。
- 介面文案仍可使用「主日」一詞（教會慣用語），但日期計算必須走設定值。

### 使用者規模（小規模、真實名單制）

- 孩子 47 位（兒童/幼童/幼幼三個班別）、家長約 26 位、老師約 20 位，總計約 100 人以內。
- 95% 以上使用者以**手機瀏覽器**（iOS / Android）操作 → **一律 mobile-first 設計**。
- 規模小、名單固定：不做公開註冊，不追求擴張性指標；優先考量簡單、穩定、零成本或低成本方案。

## 最高守則（開發前必讀）

`docs/DESIGN_PRINCIPLES.md` 是本 repo 的**最高守則**，任何功能設計必須先通過其檢核。與本平台特別相關的重點：

1. **兒童個資最小化**（紅燈規則 #6）：只蒐集必要欄位；兒童資料絕不用於商業用途、不外流。
2. **不做比較式排行**（紅燈規則 #1）：「服事經歷卡」是孩子**自己的成長軌跡**（加 6:4），不得演變成孩子之間的排名或比較。
3. **不做成癮式留存機制**（紅燈規則 #2）：出席統計是行政工具，不得用連續出席壓力、罪惡感通知逼迫使用。
4. **不羞辱、不標籤化**（紅燈規則 #7）：缺席紀錄僅供關懷 follow-up（未來的關懷警訊機制），不得公開呈現誰缺席。
5. 若設計與守則衝突：**立即停下、提出討論**，不得便宜行事。

## 角色與權限（標籤式多角色）

| 角色標籤 | 權限重點 |
| --- | --- |
| 管理者（核心同工） | 全權存取、發布教材/歌單、維護名單與角色標籤、審核服事資格 |
| 老師 | 現場簽到點名、檢視備課教材（影音+討論問題）、觀看舞蹈教學影片、登錄服事履歷 |
| 家長 | 填寫預先出席、預習下週歌單、查看自己孩子的服事卡、接收/回覆公告 |

**角色採標籤集合設計**（`profiles.roles user_role[]`）：一人可同時擁有多個角色標籤（如組長＝管理者＋老師＋家長），擁有某標籤即開通該角色功能。**嚴格逐標籤授權，角色之間沒有上下位關係、管理者也不自動涵蓋其他角色功能**（例：未來會議記錄分「老師會議紀錄（分班）」與「同工會議紀錄」，各自對應標籤）；管理者標籤對應的是名單/角色維護、公告發布、內容審核等管理功能。僅管理者可變更他人的角色標籤。

權限原則：

- 家長**只能看到自己綁定的孩子**（以 Supabase RLS 在資料庫層強制執行，不靠應用層自律）；一位家長可綁定多位孩子（跨班別），介面需支援切換或一次勾選多孩。
- 老師端分**三班共同區**與**班別個別區**（兒童/幼童/幼幼）：老師隸屬一或多個班別，個別區內容依班別隔離；核心同工可跨班存取。
- 高敏感內容（家長/孩子通訊錄、當週課後特別狀況紀錄）僅老師/核心同工可見，設計前必讀規格書「個資紅線」一節。

## 開發優先順序（Roadmap）

- **Phase 1（急用，取代現有 Google 表單流程）**：
  1. 家長預先出席勾選（每週固定開放時段，如週三前）＋自動統計預計出席人數
  2. 老師現場簽到點名
  3. 敬拜歌單/影片發布（後台上傳歌詞、音檔或 YouTube 連結；家長端主日後可預習下週內容）
  4. 家長佈告欄（公告發布）
- **Phase 2（老師營運區，來自組長需求增補 v2）**：
  5. 班別分區（三班共同區＋班別個別區）
  6. 家長/孩子名單與通訊錄（個資紅線管制）
  7. 老師服事排班表、開會決議、流程運作要點
  8. 教案/PPT（雲端連結外連）、詩歌熟悉指數（班級 × 詩歌）
- **Phase 3（文化深化）**：
  9. 服事報名表 → 電子服事經歷卡全流程（報名/受訓中/已核定/服事完成；家長端可檢視）
  10. 公告雙向互動（家長回覆/簽到）
  11. 當週課後特別狀況紀錄（高敏感，關懷交接用）
- **未來預留（不在現階段實作，但資料模型要預留）**：
  - 第三方登入：Google、LINE、Apple（見下方「登入策略」）
  - LINE 機器人/通知串接（出席提醒、歌單推播）
  - 家庭延伸祭壇（主日討論題回饋與打卡）
  - 關懷警訊（連續缺席自動標示，僅供窗口關懷用）
  - User 資料表預留欄位：生日、大孩子階級（培訓中/正式領袖）、家庭關聯 ID

## 技術選型（已決定）

| 層 | 選型 | 理由 |
| --- | --- | --- |
| 前端 | **Vue 3 + Vite + Vant**（SPA / PWA） | mobile-first UI 庫、繁中文件齊全；封閉登入制平台無 SEO 需求，SPA 最簡單 |
| 後端 | **Supabase**（Postgres + Auth + RLS） | 關聯式資料契合建模；RLS 落實家長資料隔離；Auth 可從 Email 擴充至 Google/Apple（內建）與 LINE（自訂） |
| 託管 | Vercel / Cloudflare Pages（免費額度） | 總使用者 < 100 人，免費額度長期夠用，避免固定月費 |
| 影音 | 一律**外連或嵌入 YouTube** | 不自建影音儲存/串流 |

### 登入策略

- **已實作**：Email 註冊/登入＋**Google OAuth 註冊/登入**（設定步驟見 README「Google 登入設定」）；session 由 supabase-js 持久化於 localStorage，回訪免重登。
- **註冊即家長＋審核制**：任何管道註冊的新使用者預設角色 `parent`、`approved=false`（待審核）。**未審核者僅能看公告與帳號設定**——`my_roles()` 對未審核者回空陣列，所有角色權限在資料庫層自動失效；審核與取消核准、刪除成員皆於名單頁操作（防自我審核已入 RLS）。「邀請成員」＝分享平台網址給對方自行註冊。
- **未來擴充**：Apple（Supabase 內建）、LINE（透過 Edge Function 自訂接入）。
- `auth_provider` 欄位由 `handle_new_user` 觸發器自動寫入（email / google / …）。

### 注意事項

- Supabase 免費專案 **7 天無活動會休眠**；兒主每週使用基本不會踩到，但建議加一個 GitHub Actions 每週 ping 作保險。
- 所有權限規則優先寫成 **RLS policy**；前端的角色判斷只是 UX，不是安全邊界。

## 核心資料實體（建模時參考）

`User`（含角色、`auth_provider`、聯絡欄位最小化）、`Child`（含班別、階級）、`Family`（家長-孩子關聯）、`ClassGroup`（班別：兒童/幼童/幼幼）、`TeacherAssignment`（老師-班別關聯）、`AttendancePlan`（預先出席＋家長給老師的話）、`CheckIn`（當日紀錄：簽到/臨時請假＋老師交接備註）、`SessionFeedback`（課堂表現表情，家長可見自己孩子）、`SessionLog`（課堂紀錄：日期/老師/教學內容/詩歌進度/課後反饋，兩天內填寫、全年連貫呈現、可匯出）、`Lesson`（每週教材/討論問題/帶領提示/教案連結）、`Song`／`Playlist`（敬拜歌單）、`SongFamiliarity`（班級 × 詩歌熟悉指數）、`ServiceSignup`（服事報名）、`ServiceRecord`（服事經歷＋狀態）、`TeacherSchedule`（老師服事排班）、`MeetingNote`（開會決議，可分班別）、`Announcement`／`Reply`（公告與回覆）。

**紀錄保存**：出席/簽到/學生狀況與課堂紀錄於「我的 → 出席紀錄」可查近半年並匯出 CSV（UTF-8＋BOM），供存放教會 NAS 與匯入教會 Google Sheet。

## 已確認的設計決議（2026-07-28／07-30 更新）

1. **老師文字備註不對家長顯示**：家長端只看表情回饋/指數；文字備註僅老師與教長（admin）可見。此為已確認決議，勿再更動 RLS 放寬。
2. **「有上課的老師」判定**：暫以「當天有點名動作」為代理判斷（首頁課後反饋提醒用）；待排班表功能上線後改為正式判定。
3. **Google Sheet 全自動同步：暫緩**——需等**組長通知**取得教會 Google 雲端授權（Service Account）後，才以 Supabase Edge Function 串接；在此之前一律以 CSV 手動匯出→存 NAS／匯入 Google Sheet。
4. **老師標籤班別化**（2026-07-30，PRD v1.0 裁決）：`roles` 保留 `teacher`＋新增 `teacher_class_assignments`（老師×班別）；點名/日誌等功能依班別授權（幼幼班老師不能操作兒童班），介面顯示「＊＊班老師」。
5. **出席頁＝行事曆＋嚴格家長視角**：只顯示自己綁定的孩子（老師/同工無綁定即無孩子可填）；日期可點按（預留填未來/整月）；**出席預設值維持「未定」**。
6. **專心/配合指數（Emoji 量表）家長可見（僅自己孩子）**；幼幼班點名不含指數。
7. **公告班別化**：各班老師僅能發自己班別的公告、不可跨班；admin 可發布/修改全部。
8. **詩歌**：曲庫制（可整學期上傳、依上傳時間新→舊排序）；每首歌多影片連結（詩歌＋詩歌舞蹈）；「本週/下週＊＊班的詩歌」班別標籤；歌曲/動作兩維熟悉度於日誌填寫、詩歌頁顯示（幼幼班不填）。
9. **活動花絮**：僅審核通過成員可看；儲存使用率**超過 80% 提醒同工轉移圖片至教會 Google 雲端**。
10. 完整決議與待釐清見規格書「需求增補 v3」；教案/日誌/運作流程/教材庫等組長詳細版再定。

## Repo 結構與文件慣例

```
README.md                              # 平台簡介與必讀文件索引（不放開發設定）
CLAUDE.md                              # 本文件
.claude/skills/karpathy-guidelines/    # 程式撰寫行為守則 skill（先想再寫、簡單優先、
                                       # 外科手術式修改、目標驅動；寫/改程式時遵循）
docs/DESIGN_PRINCIPLES.md              # 最高守則（設計檢核，必讀）
docs/SUNDAY_SCHOOL_PLATFORM_SPEC.md    # 需求規格書（需求基準）
docs/DEVELOPMENT.md                    # 開發指南：開發/測試/後端初始化/部署/第三方登入設定
docs/PROGRESS.md                       # 進度總覽（單一事實來源；每個 Sprint 結束必更新）
docs/SPRINT_*.md                       # 各階段開發範圍與回顧
src/                                   # 前端（views/api/stores/lib/components）
supabase/                              # schema.sql、rls.sql、seed.sql 與 migrations/
```

**文件慣例（已確認）**：最外層 `README.md` **不放開發設定**——開發、測試、部署、第三方登入、後端設定一律寫在 `docs/DEVELOPMENT.md`，README 只放專案簡介與必讀文件連結。

## 工作流程慣例

- **進度文件（必遵守）**：`docs/PROGRESS.md` 是跨 session／跨協作者的**進度單一事實來源**。**每完成一次 Sprint（或外部設定完成、需求異動時）必須更新它**——已實作打勾、尚未實作調整、外部依賴與 Sprint 歷程表同步；任何新 session 開工前應先讀它掌握現況。
- **測試**：框架為 Vitest；測試檔放在 `src/**/__tests__/*.test.ts`。新增或修改功能時**必須**同步新增/更新單元與邊際測試（日期邏輯、權限判斷尤其必要），送 PR 前跑 `npm test` 與 `npm run build` 確認全綠；CI 會於 push/PR 自動執行。
- **分支**：功能開發在 `claude/*` 或 feature 分支進行，不直接 push `main`。
- **Commit**：訊息以繁體中文為主，清楚描述「做了什麼、為什麼」。
- **機密**：`.env`、`*.secret` 一律 gitignore；任何金鑰、Supabase service key、Webhook、兒童個資都不得進版控。
- **兒童個資**：名單（孩子姓名、班別、家長關聯）只存在 Supabase 資料庫，**不放入 repo**（包含 seed 檔與測試資料——測試一律用假名）。
